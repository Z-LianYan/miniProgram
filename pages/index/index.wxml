<!--index.wxml-->
<view class="container">
	<!-- <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view> -->

	<view class="search_box">
    <view class="location_box">
      <van-icon name="location" color="#ff833b" size="20"/>
      <text>广州</text>
    </view>
    
    <van-search 
    custom-class="search_content" 
    disabled 
    placeholder="搜索热门演出" ></van-search>

    <van-icon class="calendar" name="calender-o" color="#ff833b" size="30"/>
    
  </view>
  
	<view class="content_box index_content_box_box">

		<view class="classify_list_box clearfix">
			<view wx:for="{{classifyList}}" wx:for-item="items" wx:for-index="idx" class="classify_item {{(idx+1)%5==0?'':'classify_item_margin_right'}}" wx:key="key">
				<image class="classify_image" src="{{items.pic}}" mode="cover"></image>
				<view>{{items.name}}</view>
			</view>

		</view>

  
  <view style="height:900rpx;background:red;">12312312</view>


	</view>



</view>